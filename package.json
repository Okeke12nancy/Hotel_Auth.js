{
  "name": "hotels",
  "version": "1.0.0",
  "description": "- clone the repository\r - install the dependencies: using `npm i`\r - create a new file named `.env` in root folder of the project.\r - Mongo DB atlas is advised for the `DATABASE_URL` key but local database url is fine\r   -start the server in development by running: `npm start`. Read the output from the terminal to be sure the server is running and the database is connected.",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon -r dotenv/config ./app.js",
    "start": "node ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "clean": "^4.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "helmet": "^6.0.1",
    "http-status-codes": "^2.2.0",
    "joi": "^17.8.1",
    "jsonwebtoken": "^9.0.0",
    "lodash": "^4.17.21",
    "mongoose": "^6.9.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.20",
    "pino": "^8.10.0",
    "rate-limiter": "^0.2.0",
    "xss": "^1.0.14",
    "xss-clean": "^0.1.1"
  }
}
